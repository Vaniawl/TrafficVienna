---
description: Run the autonomous TrafficVienna workflow for a concrete objective.
---

Use the orchestrator workflow in `docs/opencode/multi-agent-workflow.md`.

First read `AGENTS.md`, `docs/CONTEXT.md`, `docs/REFERENCES.md`, `memory/DECISIONS.md`, `memory/JOURNAL.md`, `docs/opencode/task-contract.md`, `docs/opencode/permission-matrix.md`, and `docs/opencode/git-ci-release.md`.

Then handle this objective with bounded delegation, checkpoints, validation, review, and draft PR handoff:

$ARGUMENTS
