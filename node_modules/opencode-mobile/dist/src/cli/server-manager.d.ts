/**
 * Server manager for starting/stopping the plugin server
 */
import type { AuditOptions, ServerInfo } from "./types.js";
/**
 * Start the server
 */
export declare function startServer(options: AuditOptions): Promise<{
    port: number;
    proxyPort: number;
}>;
/**
 * Stop the server
 */
export declare function stopServer(): Promise<void>;
/**
 * Check if server is reachable
 */
export declare function checkServer(port: number): Promise<{
    reachable: boolean;
    corsConfigured: boolean;
    responseTime: number;
}>;
/**
 * Get server info
 */
export declare function getServerInfo(port: number): Promise<ServerInfo>;
//# sourceMappingURL=server-manager.d.ts.map